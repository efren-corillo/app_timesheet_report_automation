# MySQL
MYSQL_ROOT_PASSWORD=changeme-root
MYSQL_DATABASE=appdb
MYSQL_USER=appuser
MYSQL_PASSWORD=changeme-user

# Caddy
# Use a real domain to enable HTTPS via Let's Encrypt (A/AAAA must point to this host)
CADDY_DOMAIN=app.local.test
CADDY_EMAIL=dev@local
UI_UPSTREAM=ui:8080
API_UPSTREAM=api:5000
APP_HOME=/app

LOCAL_UID=
LOCAL_GID